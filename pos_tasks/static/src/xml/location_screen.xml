<?xml version="1.0" encoding="UTF-8"?>
<templates id="template" xml:space="preserve">
<!--This is used for creating a new screen for showing booked orders-->
    <t t-name="custom_pos_screen.ProductCombosScreen">
        <div class="combo-screen screen h-100 bg-100">
            <div class="controls d-flex align-items-center justify-content-between mt-1 mt-md-0 p-2 bg-400">
                <div class="buttons d-flex gap-2">
                    <button class="discard btn btn-lg btn-light" t-on-click="() => this.pos.showScreen('ProductScreen')">
                        <span class="search-icon">
                            <i class="fa fa-angle-double-left"/>
                        </span>
                        Back
                    </button>
                </div>
            </div>

      <div class="container-md">
        <table class="table">
          <thead>
            <tr>
                 <th  scope="col" >S.No</th>
              <th  scope="col">Name</th>
              <th scope="col">Address</th>
              <th scope="col">Pincode</th>
            </tr>
          </thead>
          <tbody>
            <t t-set="i" t-value="1"/>
            <t t-foreach="props.data" t-as="order" t-key="order.id">
              <tr   t-on-click="() => this._Confirm(order)">
                <th scope="row"><t t-esc="i"/></th>
                <td><t t-esc="order.name"/></td>
                <td><t t-esc="order.address"/></td>
                <td><t t-esc="order.pin_code"/></td>
                <t t-set="i" t-value="i+1"/>
              </tr>
            </t>

          </tbody>
        </table>
      </div>
         </div>
    </t>
</templates>
